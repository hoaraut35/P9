package com.openclassrooms.realestatemanager.utils

//import androidx.compose.runtime.snapshots.SnapshotApplyResult
import androidx.room.RoomOpenHelper
import com.openclassrooms.realestatemanager.models.RealEstate

class RealEstateValidator {


  /*  fun testValid(addFormData: RealEstate) {

        var isValid = true
        var errorMessage = ""

        with(addFormData) {

            if (price.isEmpty()) {
                isValid = false
                errorMessage = "Price must not be empty."
            }
        }

        return if (isValid){
            RoomOpenHelper.ValidationResult.S = succes
        }


    }

    private fun isStrong(password: String): Boolean {
        // Credit for this regex goes to: https://stackoverflow.com/questions/5142103/regex-to-validate-password-strength
        return password.matches(Regex("^(?=.*[A-Z].*[A-Z])(?=.*[!@#\$&*])(?=.*[0-9].*[0-9])(?=.*[a-z].*[a-z].*[a-z]).{8}\$"))
    }
*/
}